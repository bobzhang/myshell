name = "bobzhang/myshell"

version = "0.2.0"

readme = "README.mbt.md"

repository = "https://github.com/bobzhang/myshell"

license = "Apache-2.0"

keywords = [ "process", "pipeline", "async", "sandbox", "agent" ]

description = "A small shell-free process EDSL for MoonBit"

preferred_target = "wasm"

import {
  "moonbitlang/async@0.20.5",
}
